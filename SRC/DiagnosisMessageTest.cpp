/*
#include "DiagnosisMessage.h"
#include <stdio.h>

int main() {
    SingleCost tempSingleCost = constructSingleCost(1145,1,4);
    char* costString =  convertSingleCostToString(tempSingleCost);
    char* dateString = "2020-2-22 02:22";
    TimeRecord tempTimeRecord = convertStringToTimeRecord(dateString);
    printf("%s\n",costString);
    printf("%d %d %d %d\n",tempTimeRecord.month, tempTimeRecord.day,
        tempTimeRecord.hour, tempTimeRecord.minute);
    printf("%s\n",convertTimeRecordToString(tempTimeRecord));
    return 0;
}
*/
